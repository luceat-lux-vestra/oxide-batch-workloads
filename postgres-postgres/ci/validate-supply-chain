#!/usr/bin/env bash
# Additional ecosystem supply-chain contract for the postgres-postgres workload.
# Cargo remains covered by the repository-wide cargo-deny validator; this hook
# fail-closes the nested Maven benchmark manifest surface.
set -euo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")/.."

python3 benchmark/java/ci/test-spring-enforcer-execution.py
python3 benchmark/java/ci/validate-spring-enforcer-execution.py
python3 benchmark/java/ci/test-spring-enforcer-policy.py
python3 benchmark/java/ci/validate-poms.py
